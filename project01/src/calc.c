#include "calc.h"
#include <stdlib.h>

/**
 * Description:
 * 
 *  avg - calculate the average value for a set of int values stored
 *        in a block of memory.   
 * 
 * Parameters:
 * 
 *  nums - a pointer to the first int stored in the set
 * 
 *  size - the number of int values stored in nums
 * 
 * Return:
 *  
 *  In order to pass the Unit Test, your return value must do the
 *  following:
 *   
 *   1. Return the average of the int values stored in nums.
 *   2. Return 0 if size < 1.
 *   3. Return 0 if nums is NULL.
 *
 */
int avg(const int* nums, int size)
{
    
}

/**
 * Description:
 * 
 *  sum - calculate the sum of the value for a set of integers stored
 *        in a block of memory. 
 * 
 * Parameters:
 * 
 *  terms - a pointer to the first int stored in the set
 * 
 *  size - the number of int values stored in terms
 * 
 * Return:
 *  
 *  In order to pass the Unit Test, your return value must do the
 *  following:
 *   
 *   1. Return the sum of the int values stored in terms.
 *   2. Return 0 if size < 1.
 *   3. Return 0 if terms is NULL.
 *
 */
int sum(const int* terms, int size)
{

}

/**
 * Description:
 * 
 *  prod - calculate the product of the value for a set of integers stored
 *        in a block of memory. 
 * 
 * Parameters:
 * 
 *  factors - a pointer to the first int stored in the set
 * 
 *  size - the number of int values stored in factors
 * 
 * Return:
 *  
 *  In order to pass the Unit Test, your return value must do the
 *  following:
 *   
 *   1. Return the product of the int values stored in nums.
 *   2. Return 0 if size < 1.
 *   3. Return 0 if factors is NULL.
 *
 */
int prod(const int* factors, int size)
{

}
